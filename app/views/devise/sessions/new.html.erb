<% content_for :hide_auth_buttons, true %>

<section class="auth-hero">
  <div class="container text-center">
    <div class="auth-hero-icon">💙</div>
    <h1 class="auth-hero-title">Hospital IA</h1>
    <p class="auth-hero-subtitle">
      Tu asistente médico virtual disponible 24/7 para consultas,<br class="d-none d-md-inline">
      recomendaciones y cuidados
    </p>

    <div class="d-flex gap-2 justify-content-center mt-3">
      <button class="btn btn-light btn-lg px-4" data-bs-toggle="modal" data-bs-target="#loginModal">
        Comenzar ahora
      </button>
      <%= link_to "Crear cuenta", new_registration_path(resource_name), class: "btn btn-outline-light btn-lg px-4" %>
    </div>
  </div>
</section>

<section class="features-section">
  <div class="container">
    <h2 class="text-center fw-bold mb-4">¿Qué puedes hacer con Hospital IA?</h2>

    <div class="row g-4 row-cols-1 row-cols-md-4">
      <div class="col">
        <div class="feature-tile">
          <div class="feature-icon">💬</div>
          <h3 class="feature-title">Consultas sobre Síntomas</h3>
          <p class="feature-desc">Pregunta sobre tus síntomas y recibe orientación médica general</p>
        </div>
      </div>

      <div class="col">
        <div class="feature-tile">
          <div class="feature-icon">🛡️</div>
          <h3 class="feature-title">Recomendaciones Previas</h3>
          <p class="feature-desc">Obtén consejos antes de tu cita médica</p>
        </div>
      </div>

      <div class="col">
        <div class="feature-tile">
          <div class="feature-icon">❤️</div>
          <h3 class="feature-title">Cuidados Post-Procedimiento</h3>
          <p class="feature-desc">Aprende sobre los cuidados necesarios después de procedimientos</p>
        </div>
      </div>

      <div class="col">
        <div class="feature-tile">
          <div class="feature-icon">⏱️</div>
          <h3 class="feature-title">Interpretación de Análisis</h3>
          <p class="feature-desc">Ayuda para entender tus resultados médicos</p>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="cta-band">
  <div class="container text-center">
    <h3 class="fw-bold mb-2">Comienza tu consulta hoy</h3>
    <p class="text-muted mb-3">Únete a miles de pacientes que confían en Hospital IA para sus consultas médicas</p>
    <%= link_to "Crear cuenta gratis", new_registration_path(resource_name), class: "btn btn-primary px-4" %>
  </div>
</section>

<footer class="auth-footer">
  <div class="container text-center small">
    <div class="mb-1">💙 Hospital IA</div>
    <div class="text-muted">Este asistente proporciona información general. Siempre consulta con un profesional médico.</div>
  </div>
</footer>

<div class="modal fade" id="loginModal" tabindex="-1" aria-hidden="true"
     data-bs-backdrop="static" data-bs-keyboard="false">
  <div class="modal-dialog modal-dialog-centered" style="max-width: 520px;">
    <div class="modal-content">
      <div class="modal-header border-0">
        <h5 class="modal-title" style="color:var(--primary)">Inicia sesión</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
      </div>
      <div class="modal-body pt-0">
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="mb-3">
            <%= f.input :email,
                        label: "Correo electrónico",
                        required: true,
                        autofocus: true,
                        input_html: { autocomplete: "email", class: "form-control form-control-lg" },
                        wrapper_html: { class: "mb-0" } %>
          </div>
          <div class="mb-2">
            <%= f.input :password,
                        label: "Contraseña",
                        required: true,
                        input_html: { autocomplete: "current-password", class: "form-control form-control-lg" },
                        wrapper_html: { class: "mb-0" } %>
          </div>

          <% if devise_mapping.rememberable? %>
            <div class="form-check mb-3">
              <%= f.input :remember_me, as: :boolean, label: "Recordarme",
                          input_html: { class: "form-check-input" },
                          label_html: { class: "form-check-label" },
                          wrapper_html: { class: "m-0 p-0" } %>
            </div>
          <% end %>

          <div class="d-grid">
            <%= f.button :submit, "Ingresar", class: "btn btn-primary btn-lg" %>
          </div>
        <% end %>

        <div class="d-flex justify-content-between align-items-center mt-3">
          <% if devise_mapping.recoverable? %>
            <%= link_to "¿Olvidaste tu contraseña?", new_password_path(resource_name), class: "small" %>
          <% end %>
          <% if devise_mapping.registerable? %>
            <span class="small">¿No tienes cuenta?
              <%= link_to "Crear cuenta", new_registration_path(resource_name) %>
            </span>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
