<div class="container py-5">
  <!-- Descripción + botón -->
  <div class="card border-0 shadow-sm mx-auto mb-4" style="max-width: 880px;">
    <div class="card-body p-4 p-md-5 text-center">
      <h1 class="h3 mb-3" style="color:var(--primary)">¿Qué es Hospital IA?</h1>
      <p class="lead mb-3">
        <strong>Hospital IA</strong> es un software web para pacientes que integra
        autenticación segura, un panel de salud personal y un asistente de inteligencia
        artificial para orientación previa y posterior a tu cita. No sustituye la consulta
        médica, pero te ayuda a entender indicaciones y llegar mejor preparado.
      </p>
      <div class="row g-3 text-start mb-3">
        <div class="col-md-6">
          <ul class="list-unstyled mb-0">
            <li class="mb-2">• Preguntas sobre <em>síntomas</em> y preparación.</li>
            <li class="mb-2">• <em>Cuidados</em> pre y post-procedimiento.</li>
            <li class="mb-2">• Subida de <em>análisis</em> para orientación general.</li>
          </ul>
        </div>
        <div class="col-md-6">
          <ul class="list-unstyled mb-0">
            <li class="mb-2">• Solicitud y seguimiento de <em>citas</em>.</li>
            <li class="mb-2">• Perfil médico editable.</li>
            <li class="mb-2">• Chatbot con historial.</li>
          </ul>
        </div>
      </div>
      <small class="text-muted d-block mb-4">
        ⚠️ La información del asistente es orientativa y no sustituye una consulta médica presencial.
      </small>

      <!-- Botón que abre el modal -->
      <button class="btn btn-primary btn-lg px-4"
              data-bs-toggle="modal"
              data-bs-target="#loginModal">
        Ingresar
      </button>
    </div>
  </div>
</div>

<!-- Modal con el formulario de login -->
<div class="modal fade" id="loginModal" tabindex="-1" aria-hidden="true"
     data-bs-backdrop="static" data-bs-keyboard="false">
  <div class="modal-dialog modal-dialog-centered" style="max-width: 520px;">
    <div class="modal-content">
      <div class="modal-header border-0">
        <h5 class="modal-title" style="color:var(--primary)">Inicia sesión</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
      </div>
      <div class="modal-body pt-0">
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="mb-3">
            <%= f.input :email,
                        label: "Correo electrónico",
                        required: true,
                        autofocus: true,
                        input_html: { autocomplete: "email", class: "form-control form-control-lg" },
                        wrapper_html: { class: "mb-0" } %>
          </div>
          <div class="mb-2">
            <%= f.input :password,
                        label: "Contraseña",
                        required: true,
                        input_html: { autocomplete: "current-password", class: "form-control form-control-lg" },
                        wrapper_html: { class: "mb-0" } %>
          </div>

          <% if devise_mapping.rememberable? %>
            <div class="form-check mb-3">
              <%= f.input :remember_me, as: :boolean, label: "Recordarme",
                          input_html: { class: "form-check-input" },
                          label_html: { class: "form-check-label" },
                          wrapper_html: { class: "m-0 p-0" } %>
            </div>
          <% end %>

          <div class="d-grid">
            <%= f.button :submit, "Ingresar", class: "btn btn-primary btn-lg" %>
          </div>
        <% end %>

        <div class="d-flex justify-content-between align-items-center mt-3">
          <% if devise_mapping.recoverable? %>
            <%= link_to "¿Olvidaste tu contraseña?", new_password_path(resource_name), class: "small" %>
          <% end %>
          <% if devise_mapping.registerable? %>
            <span class="small">¿No tienes cuenta?
              <%= link_to "Crear cuenta", new_registration_path(resource_name) %>
            </span>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
